import { View, Text } from "react-native";
import { SafeAreaView } from 'react-native-safe-area-context';
import React from "react";

const Saved = () => {
    return (
        <SafeAreaView>
            <View >
                <Text>This is the Saved component</Text>
            </View>
        </SafeAreaView>

    )
}

export default Saved;
